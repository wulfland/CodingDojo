name: test-workflow

on: 
  workflow_dispatch:
  
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo $MY_SECRET
      - run: echo $X
        env:
          X: ${{secrets.MY_SECRET}}
