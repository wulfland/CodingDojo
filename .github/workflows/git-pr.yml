name: git-pr

on:
  pull_request:
    paths: solutions/git/**

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
        
      - name: Run a one-line script
        run: echo Hello, world!
